<h2>Lista de categorias</h2>
<hr>
{{#each categorias}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{nome}}</h4>
            <small>Slug: {{slug}}</small> <br />
            <small>Date de criação {{date}}</small>
        </div>
    </div>
    {{else}}
    
{{/each}}
<a href="/admin/categorias/add">
    <button class="btn btn-success">Nova categoria</button>
</a>